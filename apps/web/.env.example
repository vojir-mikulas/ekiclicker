# Zkopíruj do .env (gitignored) a uprav. Vite čte VITE_* při buildu/startu.

# Konzolové cheaty (window.eki.setMoney/setLevel/dropItem/unlock).
# 1 = zapnuto; cokoliv jiného nebo nenastaveno = vypnuto a Vite kód odstraní z bundle.
# Změna se projeví až po: prod → `npm run build`, dev → restart `npm run dev`.
VITE_CHEATS=0
